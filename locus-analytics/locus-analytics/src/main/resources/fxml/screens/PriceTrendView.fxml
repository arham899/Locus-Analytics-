<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.chart.CategoryAxis?>
<?import javafx.scene.chart.LineChart?>
<?import javafx.scene.chart.NumberAxis?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.ComboBox?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.GridPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.Priority?>
<?import javafx.scene.layout.VBox?>

<VBox spacing="16.0" styleClass="terminal-screen-root" xmlns="http://javafx.com/javafx/21"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.locus.ui.controller.screen.PriceTrendController">
    <padding>
        <Insets top="18.0" right="18.0" bottom="18.0" left="18.0"/>
    </padding>

    <VBox spacing="4.0">
        <Label text="Price Trends Overview" styleClass="terminal-screen-title"/>
        <Label text="Monitor quarterly movement, demand pressure, and velocity shifts across major zones." styleClass="terminal-muted-text"/>
    </VBox>

    <HBox spacing="12.0">
        <VBox spacing="5.0" HBox.hgrow="ALWAYS">
            <Label text="City" styleClass="terminal-label"/>
            <ComboBox fx:id="cityComboBox" styleClass="stitch-input" maxWidth="1.7976931348623157E308"/>
        </VBox>
        <VBox spacing="5.0" HBox.hgrow="ALWAYS">
            <Label text="Property Type" styleClass="terminal-label"/>
            <ComboBox fx:id="propertyTypeComboBox" styleClass="stitch-input" maxWidth="1.7976931348623157E308"/>
        </VBox>
        <VBox spacing="5.0">
            <Label text="Actions" styleClass="terminal-label"/>
            <Button text="Refresh Trend" styleClass="terminal-primary-button" onAction="#onLoadTrend"/>
        </VBox>
    </HBox>

    <GridPane hgap="12.0" vgap="12.0">
        <VBox spacing="5.0" styleClass="terminal-kpi-card" GridPane.columnIndex="0" GridPane.rowIndex="0">
            <Label text="ANNUAL APPRECIATION" styleClass="terminal-kpi-label"/>
            <Label fx:id="annualAppreciationLabel" text="-" styleClass="terminal-kpi-value"/>
        </VBox>
        <VBox spacing="5.0" styleClass="terminal-kpi-card" GridPane.columnIndex="1" GridPane.rowIndex="0">
            <Label text="HIGHEST PRICE" styleClass="terminal-kpi-label"/>
            <Label fx:id="highestPriceLabel" text="-" styleClass="terminal-kpi-value"/>
        </VBox>
        <VBox spacing="5.0" styleClass="terminal-kpi-card" GridPane.columnIndex="2" GridPane.rowIndex="0">
            <Label text="LOWEST PRICE" styleClass="terminal-kpi-label"/>
            <Label fx:id="lowestPriceLabel" text="-" styleClass="terminal-kpi-value"/>
        </VBox>
    </GridPane>

    <VBox spacing="10.0" styleClass="terminal-card" VBox.vgrow="ALWAYS">
        <HBox>
            <Label text="Quarterly Price Curve" styleClass="terminal-card-title"/>
        </HBox>
        <LineChart fx:id="trendChart" createSymbols="false" VBox.vgrow="ALWAYS" minHeight="320.0">
            <xAxis>
                <CategoryAxis label="Quarter"/>
            </xAxis>
            <yAxis>
                <NumberAxis label="Average Price (PKR)"/>
            </yAxis>
        </LineChart>
    </VBox>

    <Label fx:id="statusLabel" text="-" styleClass="terminal-status-label"/>
</VBox>
