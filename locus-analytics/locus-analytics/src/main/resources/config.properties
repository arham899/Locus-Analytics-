# ─────────────────────────────────────────────
# PostgreSQL — Analytics data (property, valuation, rental, ROI, trends, clusters, ETL)
# ─────────────────────────────────────────────
pg.db.url=jdbc:postgresql://localhost:5432/locus_analytics
pg.db.username=postgres
pg.db.password=password

# ─────────────────────────────────────────────
# Oracle — Enterprise data (users, system configuration, audit logs)
# ─────────────────────────────────────────────
oracle.db.url=jdbc:oracle:thin:@localhost:1521:xe
oracle.db.username=system
oracle.db.password=password

# GOOGLE MAPS (Phase 2 - Ayaan will use this)
google.maps.api.key=REPLACE_WITH_API_KEY

# ML MODEL (Phase 2 - Arham will generate this)
model.file.path=REPLACE_WITH_MODEL_PATH